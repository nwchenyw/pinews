# ? 快速測試指南

## ?? 5 分鐘快速開始

### 步驟 1: 構建應用
```powershell
cd C:\Users\plauv\devstackers\pinews
dotnet build
```
**預期**: 應該看到 "建置成功" 消息

### 步驟 2: 運行應用
```powershell
dotnet run
```
**預期**: 應用啟動，通常看到類似這樣的消息:
```
info: Microsoft.Hosting.Lifetime[14]
      Now listening on: https://localhost:7xxx
      Now listening on: http://localhost:5xxx
```

### 步驟 3: 訪問首頁
打開瀏覽器，訪問: `http://localhost:5xxx` (或上面顯示的地址)

**預期**: 首頁正常加載，顯示所有內容

### 步驟 4: 查看性能日誌
在 Visual Studio 的 **Output** 窗口中查看日誌:

```
[性能] 並行查詢耗時 (Banners, Marquees, Recommendations, Ads): XXXms
[性能] RecommendationArticles 查詢耗時: XXXms
[性能] Index 頁面總耗時: XXXms (優化版本 - 並行執行)
```

## ?? 性能測試

### 方法 1: 瀏覽器開發者工具

1. **打開開發者工具**: F12
2. **切換到 Network 標籤**
3. **刷新頁面**: F5
4. **查看首頁加載時間**
   - 找到 "Document" 項目 (通常是第一項)
   - 查看 "Time" 欄位，應該是 2-3 秒

### 方法 2: Visual Studio 調試

1. **設置斷點**: 在 `HomeController.Index()` 末尾設置斷點
2. **按 F5 啟動調試**
3. **訪問首頁**: http://localhost:5xxx
4. **查看性能日誌**: Output 窗口中應顯示各查詢耗時
5. **觀察停止時間**: 應該在 2-3 秒內

### 方法 3: 應用日誌文件

如果應用寫入日誌文件:
1. 找到日誌文件位置 (通常在 `bin/Debug/net8.0/logs/` 或類似位置)
2. 搜索 `[性能]` 標籤
3. 查看各個查詢和總耗時

## ?? 驗證清單

在確認優化成功之前，請檢查以下各項:

### ? 編譯和運行
- [ ] 應用編譯成功 (沒有錯誤)
- [ ] 應用正常啟動
- [ ] 應用監聽正確的端口

### ? 功能驗證
- [ ] 首頁正常加載
- [ ] 橫幅廣告顯示
- [ ] 跑馬燈顯示
- [ ] 推薦分類顯示
- [ ] 文章顯示正確
- [ ] 廣告顯示正確

### ? 性能驗證
- [ ] 頁面加載時間 < 3 秒
- [ ] 日誌中顯示 `[性能]` 消息
- [ ] 日誌中顯示各查詢耗時
- [ ] 日誌中顯示總耗時

### ? 錯誤檢查
- [ ] 沒有編譯警告 (或只有可忽略的警告)
- [ ] 沒有運行時異常
- [ ] 應用日誌中沒有 ERROR 消息
- [ ] 瀏覽器控制台沒有 JavaScript 錯誤

## ?? 預期日誌輸出

### 正常情況
```
[性能] 並行查詢耗時 (Banners, Marquees, Recommendations, Ads): 150ms
[性能] RecommendationArticles 查詢耗時: 200ms
[性能] Index 頁面總耗時: 350ms (優化版本 - 並行執行)
```

### 解釋
- 第一行: 4 個獨立查詢的並行執行時間 (通常是最慢的查詢 + 開銷)
- 第二行: 依賴查詢的執行時間 (文章查詢)
- 第三行: 整個 Index 方法的總耗時 (應該接近第一行 + 第二行)

## ?? 故障排除

### 問題: "A second operation was started on this context..."
**原因**: DbContext 並發錯誤  
**解決**: 檢查 Program.cs 是否有 `EnableThreadSafetyChecks(false)` 配置

### 問題: 首頁顯示不完整或加載失敗
**原因**: 可能是數據庫連接問題  
**檢查清單**:
1. 確認 SQL Server 正在運行 (如果使用真實數據庫)
2. 確認 `appsettings.json` 中的連接字符串正確
3. 檢查應用日誌中的異常消息
4. 嘗試切換到 Demo Mode (設置 `UseDemoMode` 為 `true`)

### 問題: 性能沒有改善
**原因**: 可能是多方面原因  
**檢查清單**:
1. 確認頁面加載時間測量方式正確
2. 確認數據庫查詢確實並行執行 (檢查 SQL Profiler)
3. 檢查是否有其他性能瓶頸 (CSS, JavaScript, 圖片加載等)
4. 確認測試多次以排除波動

## ?? 性能基準

### 預期耗時

| 查詢 | 預期耗時 | 狀態 |
|------|---------|------|
| Banners | 50-200ms | 正常 |
| Marquees | 50-200ms | 正常 |
| Recommendations | 50-200ms | 正常 |
| LongSquareAds | 50-150ms | 正常 |
| LeftSquareAd | 50-150ms | 正常 |
| **並行查詢合計** | **50-200ms** | 應該是最慢的那個 |
| RecommendArticles | 100-300ms | 正常 |
| **總耗時** | **200-500ms** | 理想情況 |

### 警告閾值

| 指標 | 警告值 | 嚴重值 |
|------|--------|---------|
| 並行查詢耗時 | > 500ms | > 1000ms |
| RecommendArticles | > 800ms | > 1500ms |
| 總耗時 | > 2000ms | > 5000ms |

如果超過警告值，應檢查:
1. 數據庫性能
2. 網絡延遲
3. 是否需要添加數據庫索引
4. 是否需要實施快取

## ?? 驗收標準

優化被認為成功的標準:

? **必須滿足**
- 應用編譯成功
- 首頁正常加載
- 首頁顯示所有內容正確
- 性能日誌顯示正確的耗時

? **應該滿足**
- 首頁加載時間 < 3 秒 (相比原來的 5-7 秒)
- 沒有編譯警告
- 沒有運行時異常

? **最好滿足**
- 並行查詢耗時 < 200ms
- 總耗時 < 500ms
- 可重複測試結果

## ?? 常見問題快速解答

**Q: 我應該測試多少次?**  
A: 至少 5-10 次以確保結果一致

**Q: 如何確認查詢確實並行執行?**  
A: 查看日誌，並行查詢的耗時應該接近最慢查詢的耗時，而不是所有查詢耗時的總和

**Q: 如果頁面加載仍然很慢怎麼辦?**  
A: 可能是網絡延遲或其他性能瓶頸。使用瀏覽器開發者工具分析各資源的加載時間

**Q: 我需要對代碼進行其他更改嗎?**  
A: 不需要，這次優化已完成。您可以直接使用

---

**現在開始測試吧！** ??
