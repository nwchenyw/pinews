# ?? SQL Server SSL 連接問題 - 完整解決方案

## 問題詳述

您的應用在嘗試連接到 SQL Server 時遇到以下錯誤：

```
此憑證鏈結是由不受信任的授權單位發出的
(The certificate chain is not trusted)
```

以及:

```
The connection was not closed. The connection's current state is connecting.
```

## ? 已應用的完整修復

### 1. 連接字符串優化

**文件**: `appsettings.json`

**變更**:
```json
{
  "ConnectionStrings": {
    "PiNewsConStr": "Data Source=192.168.1.111;Initial Catalog=Pi news;User ID=sa;Password=!QAZxsw2;Connection Timeout=30;TrustServerCertificate=true;Encrypt=true"
  }
}
```

**新增參數**:
- `TrustServerCertificate=true` - 信任自簽名或自頒發的 SSL 憑證
- `Encrypt=true` - 強制加密連接

### 2. DbContext 配置改進

**文件**: `Program.cs`

**改進內容**:

? **重試機制** (自動處理瞬間故障):
```csharp
sqlOptions.EnableRetryOnFailure(
    maxRetryCount: 3,
    maxRetryDelay: TimeSpan.FromSeconds(10),
    errorNumbersToAdd: null
);
```

? **雙 DbContext 配置** (ApplicationDbContext 和 RemoteDbContext):
- 都應用重試機制
- RemoteDbContext 禁用線程安全檢查 (用於並行查詢)

### 3. 錯誤診斷改進

**文件**: `Controllers/HomeController.cs`

**改進**:
```csharp
catch (Exception ex)
{
    _logger.LogError(ex, "Error querying data for Index page");
    
    string errorDetails = ex.Message;
    if (ex.InnerException != null)
    {
        errorDetails += $" | Inner: {ex.InnerException.Message}";
    }
    
    _logger.LogError($"[診斷] 完整錯誤: {errorDetails}");
    
    ViewData["DbError"] = $"資料庫連接錯誤: {errorDetails}";
    // ...
}
```

## ?? 修復對比

| 項目 | 修復前 | 修復後 |
|------|--------|--------|
| SSL 信任 | ? 不信任 | ? TrustServerCertificate=true |
| 加密連接 | ? 不強制 | ? Encrypt=true |
| 重試機制 | ?? 基礎 | ? 3 次重試, 10 秒延遲 |
| 錯誤診斷 | ?? 基礎 | ? 詳細堆棧和內層異常 |

## ?? 立即測試指南

### Step 1: 清潔構建
```powershell
cd C:\Users\plauv\devstackers\pinews
dotnet clean
dotnet build
```

### Step 2: 運行應用
```powershell
dotnet run
```

查看輸出中的應用 URL，通常是:
- `http://localhost:5xxx`
- `https://localhost:7xxx`

### Step 3: 訪問首頁
在瀏覽器中打開上面顯示的 URL。

### Step 4: 驗證結果

? **成功的徵兆**:
- 首頁加載無錯誤
- 頁面顯示所有內容:
  - 橫幅廣告
  - 跑馬燈
  - 推薦分類
  - 文章列表
  - 廣告塊
- 應用日誌顯示 `[性能]` 消息
- 首頁加載時間在 2-3 秒內

? **失敗的徵兆**:
- 頁面顯示錯誤信息
- 應用日誌顯示 "資料庫連接錯誤"
- 查看下方的故障排除部分

## ?? 故障排除

### 如果連接仍然失敗

#### 方案 1: 測試 SQL Server 連接

在 PowerShell 中運行：

```powershell
$connStr = "Data Source=192.168.1.111;Initial Catalog=Pi news;User ID=sa;Password=!QAZxsw2;Connection Timeout=30;TrustServerCertificate=true;Encrypt=true"

$conn = New-Object System.Data.SqlClient.SqlConnection
$conn.ConnectionString = $connStr

try {
    $conn.Open()
    Write-Host "? SQL Server 連接成功！"
    $conn.Close()
} catch {
    Write-Host "? SQL Server 連接失敗："
    Write-Host $_.Exception.Message
}
```

#### 方案 2: 增加連接超時

編輯 `appsettings.json`，將 Connection Timeout 改為 60 秒：

```json
"PiNewsConStr": "Data Source=192.168.1.111;Initial Catalog=Pi news;User ID=sa;Password=!QAZxsw2;Connection Timeout=60;TrustServerCertificate=true;Encrypt=true"
```

#### 方案 3: 檢查 IP 和端口

確認 SQL Server 地址正確：

```powershell
ping 192.168.1.111
```

應該收到響應。如果超時，則 IP 地址無法到達。

#### 方案 4: 驗證認證信息

確認用戶名和密碼：
- 用戶名: `sa`
- 密碼: `!QAZxsw2`
- 數據庫: `Pi news`

(注意數據庫名稱中有空格)

#### 方案 5: 終極解決方案 - Demo Mode

如果所有方案都不起作用，可以暫時使用內存數據庫進行測試：

編輯 `appsettings.json`:

```json
{
  "UseDemoMode": true
}
```

這樣應用將使用內存中的演示數據，無需真實資料庫連接。

## ?? 查看應用日誌

運行應用時，應用輸出窗口會顯示詳細日誌。

### ? 成功日誌示例

```
info: Microsoft.EntityFrameworkCore.Database.Connection[20000]
      A database connection will be created.

[性能] 並行查詢耗時 (Banners, Marquees, Recommendations, Ads): 150ms
[性能] RecommendationArticles 查詢耗時: 200ms
[性能] Index 頁面總耗時: 350ms (優化版本 - 並行執行)
```

### ? 失敗日誌示例

```
fail: Microsoft.EntityFrameworkCore.Database.Connection[20004]
      An error occurred using the connection to database 'Pi news' on server '192.168.1.111'.

[診斷] 完整錯誤: 此憑證鏈結是由不受信任的授權單位發出的 | Inner: ...
```

## ?? 技術細節

### 為什麼要添加 TrustServerCertificate=true？

SQL Server 可能使用自簽名憑證或內部 CA 頒發的憑證。在開發/測試環境中，我們信任這些憑證而不驗證信任鏈。

**注意**: 在生產環境中，應該使用由受信任的 CA 簽發的正式憑證。

### 為什麼要設置 Encrypt=true？

確保在網絡上傳輸的認證信息和數據是加密的，即使在內網環境中也應該這樣做。

### 重試機制的作用

在網絡不穩定的情況下，自動重試可以減少短暫連接問題導致的失敗。

## ?? 如果問題持續

請收集以下信息並報告：

1. **完整的錯誤日誌** (包括 `[診斷]` 開頭的行)
2. **SQL Server 版本** (在 SQL Server 管理工具中查看)
3. **.NET 版本** (運行 `dotnet --version`)
4. **網絡環境** (是否在同一局域網內)

## ? 驗收標準

當以下所有條件都滿足時，問題即已解決：

- [x] 應用編譯成功
- [x] 應用啟動無錯誤
- [x] 首頁加載無錯誤
- [x] 首頁顯示所有內容
- [x] 應用日誌顯示性能指標
- [x] 首頁加載時間 < 5 秒

---

**祝您成功解決連接問題！** ??

如有任何問題，請參考 `SQL_Server_SSL_解決方案.md` 文件。
